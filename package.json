{
  "name": "@git-lfs-hub/e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "tsgo --noEmit -p tsconfig.json",
    "test-local": "GH_PAT=$(gh auth token) RUN_ID=$(date +%s) bun e2e-test",
    "e2e-test": "bunx --bun vitest run --reporter=verbose --reporter=github-actions",
    "e2e-test:watch": "bunx --bun vitest watch"
  },
  "packageManager": "bun@1.3.13",
  "engines": {
    "node": "^26.1.0"
  },
  "dependencies": {
    "@git-lfs-hub/lib": "file:../lib"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "@typescript/native-preview": "^7.0.0-dev.20260602.1",
    "vitest": "^4.1.7"
  }
}
